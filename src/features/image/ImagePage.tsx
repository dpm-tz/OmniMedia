export function ImagePage() {
  return (
    <div className="mx-auto max-w-3xl p-8">
      <h1 className="text-2xl font-semibold">Image / Annotate</h1>
      <p className="mt-2 text-sm text-surface-200/60">
        Screenshot capture and annotation (blur, arrows, text). Implemented in
        Step 4 using `tiny-skia` + `imageproc`.
      </p>
    </div>
  );
}
