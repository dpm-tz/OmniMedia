//! Video editing pipeline. Filled in during Step 3 (FFmpeg-based timeline,
//! trim/merge/overlay). Kept as an empty module for now so the layout is
//! stable and we don't have to move files around later.
